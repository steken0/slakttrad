import React from "react";
import { Typography, Container } from "@mui/material";

const PrintPage = () => {
  return (
    <Container>
      <Typography variant="h1">Print Header</Typography>
    </Container>
  );
};

export default PrintPage;
